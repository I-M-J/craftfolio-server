# 🛖 Craftfolio — Backend Server

![Node.js](https://img.shields.io/badge/Node.js-TypeScript-green?logo=node.js)
![Express.js](https://img.shields.io/badge/Express.js-Backend-blue)
![MongoDB](https://img.shields.io/badge/MongoDB-Native_Driver-47A248?logo=mongodb)

The Express + MongoDB REST API powering the Craftfolio artisan marketplace. Handles authentication verification, item management, reviews, and statistics aggregations.

## ✨ Key Features

- **Secure JWT Verification**: Uses `jose-cjs` to verify tokens generated by Better-Auth via JWKS endpoint.
- **Role-Based Access Control**: `verifyToken` + `verifyAdmin` middleware chains protect admin-only routes.
- **Full Item CRUD**: Search, filter by category/price, sort, and paginate items.
- **Review System**: Post reviews with automatic avgRating recalculation on the item.
- **Stats Aggregation**: MongoDB `$group` pipeline returns category-wise item counts for Recharts visualization.
- **Auto-Seeding**: Seeds 12 sample items and admin account on first startup if DB is empty.

## 🛠️ Tech Stack

- **Runtime & Framework**: Node.js (TypeScript) & Express.js
- **Database**: MongoDB (Native Driver — no Mongoose)
- **Auth**: JWT verification via `jose-cjs` + Better-Auth JWKS
- **Dev**: `tsx` + `nodemon` for hot-reload TypeScript execution

## 📦 Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Create `.env` file:
   ```env
   PORT=5000
   MONGODB_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/
   CLIENT_URL=http://localhost:3000
   ADMIN_EMAIL=admin@craftfolio.com
   ADMIN_PASSWORD=Admin@1234
   ```

3. Start dev server:
   ```bash
   npm run dev
   ```

## 📖 API Documentation

### Health Check
- `GET /` — Server status

### 👥 Users
- `GET /users` — All users *(Admin only)*
- `GET /users/me` — Current user profile *(Auth required)*
- `POST /users` — Create/sync user on login
- `PATCH /users/:id/role` — Update role *(Admin only)*
- `DELETE /users/:id` — Delete user *(Admin only)*

### 🧺 Items
- `GET /items` — List items with `?search`, `?category`, `?minPrice`, `?maxPrice`, `?sortBy`, `?page`, `?limit`
- `GET /items/featured` — 8 highest-rated items for homepage
- `GET /items/:id` — Single item detail
- `POST /items` — Add item *(Auth required)*
- `PATCH /items/:id` — Update item *(Auth + owner check)*
- `DELETE /items/:id` — Delete item *(Auth + owner/admin check)*

### ⭐ Reviews
- `GET /reviews?itemId=` — Reviews for an item
- `POST /reviews` — Post review, recalculates avgRating *(Auth required)*
- `DELETE /reviews/:id` — Delete review *(Auth + owner/admin check)*

### 📊 Stats
- `GET /stats` — Category-wise item count aggregation for Recharts

## 🔐 Demo Credentials

| Role  | Email                      | Password    |
|-------|----------------------------|-------------|
| User  | demo@craftfolio.com        | Demo@1234   |
| Admin | admin@craftfolio.com       | Admin@1234  |
